# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Twilio SDK
twilio==8.12.0

# Anthropic Claude SDK
anthropic==0.18.1

# HTTP client for async requests
httpx==0.26.0

# Environment variables
python-dotenv==1.0.0

# Database (SQLite is built-in, but we need SQLAlchemy)
sqlalchemy==2.0.25

# Data validation (updated for Python 3.13 compatibility)
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Logging
structlog==24.1.0

# Distance calculation (for pickup/delivery distance)
geopy==2.4.1
